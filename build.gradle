buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.2.2'
    }
}

// We deleted the "allprojects" block from here because settings.gradle already manages it!

task clean(type: Delete) {
    delete rootProject.buildDir
}
